#include <iostream>
#include <string>
#include <cstdlib>
#include <ctime>
#include <cmath>
#include <iomanip>

using namespace std;

void quiz01_ec()
{
    cout << "Programing is fun! \n";
}
void quiz02_ec()
{
    long byte = 8;
    long gigabyte = pow(2,30) * byte ;
    long answer = 2 * gigabyte; 
    cout << answer << endl;
    cout << pow(2,34)<< endl;
}

int main() {

    cout <<  "Please enter the quiz number (106) for the extra";
    cout << "credit you would like to see: "; 
    cin >> choice; 

    switch (choice)
    {
       case 1 : cout << "Choice 1\n"';
                 break;
       case 1 : cout << "Choice 1\n"';
                 break;
       case 1 : cout << "Choice 1\n"';
                 break;           
       case 1 : cout << "Choice 1\n"';
                 break;
       case 1 : cout << "Choice 1\n"';
                 break;
       case 1 : cout << "Choice 1\n"';
                 break;                    
    }
    
}