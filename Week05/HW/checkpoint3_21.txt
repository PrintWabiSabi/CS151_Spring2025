Assume the variables angle1 and angle2 hold angles stored in radians. Write
a statement that adds the sine of angle1 to the cosine of angle2 and stores the
result in the variable x.

ANSWER BELOW
*******************************************************************************
#include <iostream>
#include <iomanip>

using namespace std;
int main()
{
double angle1, angle2
cout << "Enter angle1 (sin) and angle2 (cos)" << endl;
cin >> angle1 >> angle2 >> endl;
x = sin(angle1) + cos(angle2);
cout << " The answer is " << x << endl;

return 
0 
}
